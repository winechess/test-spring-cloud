spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/config-repo
#          uri: https://github.com/winechess/test-spring-cloud
#          searchPaths: config-repo
server:
  port: 8888
management:
  security:
    enabled: false
encrypt:
  key: TEST
security:
  basic:
    enabled: false